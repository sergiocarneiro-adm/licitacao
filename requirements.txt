# ─────────────────────────────────────────────────────────────
#  Extrator BLL Compras — Teixeira de Freitas
#  Python >= 3.9 recomendado
# ─────────────────────────────────────────────────────────────

# Automação de browser (web scraping)
# Versão fixada para garantir compatibilidade com o Chromium instalado
playwright==1.49.0

# Geração de arquivos Excel (.xlsx)
openpyxl>=3.1.5

# ─────────────────────────────────────────────────────────────
#  Dependências transitivas do playwright (instaladas
#  automaticamente pelo pip, listadas aqui por transparência)
# ─────────────────────────────────────────────────────────────
pyee>=13.0.0,<14
greenlet>=3.1.1,<4.0.0
typing-extensions>=4.0.0

# ─────────────────────────────────────────────────────────────
#  Módulos usados no projeto — todos da stdlib, sem instalação:
#    asyncio, json, os, re, shutil, subprocess, sys, datetime,
#    collections, urllib.parse
# ─────────────────────────────────────────────────────────────

# ─────────────────────────────────────────────────────────────
#  INSTALAÇÃO:
#
#  1. Criar e ativar o ambiente virtual:
#     python -m venv venv
#     venv\Scripts\activate          # Windows
#     source venv/bin/activate        # Linux/Mac
#
#  2. Instalar dependências:
#     pip install -r requirements.txt
#
#  3. Instalar os browsers do Playwright (obrigatório, uma vez):
#     playwright install chromium
#
#  4. Rodar o extrator:
#     python extrator_bll_teixeira.py
#
#  Xvfb (apenas Linux/GitHub Actions — não instale no Windows):
#     sudo apt-get install -y xvfb
# ─────────────────────────────────────────────────────────────
